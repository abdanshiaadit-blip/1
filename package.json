{
  "name": "human",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "HUMAN \u2014 India-first preventive healthcare and personal health intelligence platform",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "portable": "tsc -b && vite build --config vite.config.portable.ts && node scripts/build-portable.mjs",
    "typecheck": "tsc -b --noEmit",
    "dev:site": "vite --config vite.config.site.ts",
    "build:site": "tsc -b && vite build --config vite.config.site.ts",
    "preview:site": "vite preview --config vite.config.site.ts",
    "portable:site": "tsc -b && vite build --config vite.config.site-portable.ts && node scripts/build-portable.mjs --in dist-site-portable --out HUMAN-website --readme portable-site-README.txt"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
