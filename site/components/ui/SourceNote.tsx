/** §6.6 — every statistic on the site carries one, directly beneath it. */
export function SourceNote({ children }: { children: React.ReactNode }) {
  return <p className="t-caption source-note">{children}</p>
}
